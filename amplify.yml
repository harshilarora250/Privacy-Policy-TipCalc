version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build process required for static HTML"
    build:
      commands:
        - echo "Building static site..."
    postBuild:
      commands:
        - echo "Build completed"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
